import { ref } from "vue";

// export const baseAPI = "https://cyber-tuition-backend.onrender.com";
export const baseAPI = "http://localhost:3001";

export const pelajar = ref([]);

export const guru = ref([]);

export const kemaskiniPelajar = ref({});

export const pengesahan = ref({});

export const kemaskiniGuru = ref({});

export const kemaskiniKelas = ref({});

export const kelas = ref([]);

export const kemaskiniSubjek = ref([]);
